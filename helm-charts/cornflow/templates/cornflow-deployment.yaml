{{/*
 Licensed to the Apache Software Foundation (ASF) under one
 or more contributor license agreements.  See the NOTICE file
 distributed with this work for additional information
 regarding copyright ownership.  The ASF licenses this file
 to you under the Apache License, Version 2.0 (the
 "License"); you may not use this file except in compliance
 with the License.  You may obtain a copy of the License at

   http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing,
 software distributed under the License is distributed on an
 "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 KIND, either express or implied.  See the License for the
 specific language governing permissions and limitations
 under the License.
*/}}

################################
## Cornflow Deployment
#################################

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-cornflow-server
  labels:
    {{- include "cornflow.labels" . | nindent 4 }}
    app.kubernetes.io/component: cornflow-server
spec:
  {{- if not .Values.cornflow.autoscaling.enabled }}
  replicas: {{ .Values.cornflow.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "cornflow.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: cornflow-server
  template:
    metadata:
      labels:
        {{- include "cornflow.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: cornflow-server
      {{- with .Values.cornflow.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "cornflow.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.security | nindent 8 }}

      containers:
        - name: cornflow-server
          securityContext:
            allowPrivilegeEscalation: false
            {{- if hasKey .Values.cornflow "readOnlyRootFilesystem" }}
            readOnlyRootFilesystem: {{ .Values.cornflow.readOnlyRootFilesystem }}
            {{- else }}
            readOnlyRootFilesystem: true
            {{- end }}
            runAsNonRoot: true
            runAsUser: {{ .Values.security.runAsUser }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default (printf "release-v%s" .Chart.AppVersion) }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          {{- if not (eq false ((.Values.cornflow.probes.startup).enabled)) }}
          startupProbe:
            httpGet:
              path: {{ .Values.cornflow.probes.path | default "/health/" | quote }}
              port: http
            periodSeconds: {{ (.Values.cornflow.probes.startup).periodSeconds | default 10 }}
            timeoutSeconds: {{ (.Values.cornflow.probes.startup).timeoutSeconds | default 5 }}
            failureThreshold: {{ (.Values.cornflow.probes.startup).failureThreshold | default 18 }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.cornflow.probes.path | default "/health/" | quote }}
              port: http
            {{- if eq false ((.Values.cornflow.probes.startup).enabled) }}
            initialDelaySeconds: 120
            {{- end }}
            periodSeconds: {{ (.Values.cornflow.probes.liveness).periodSeconds | default 10 }}
            timeoutSeconds: {{ (.Values.cornflow.probes.liveness).timeoutSeconds | default 5 }}
            failureThreshold: {{ (.Values.cornflow.probes.liveness).failureThreshold | default 3 }}
          readinessProbe:
            httpGet:
              path: {{ .Values.cornflow.probes.path | default "/health/" | quote }}
              port: http
            initialDelaySeconds: {{ (.Values.cornflow.probes.readiness).initialDelaySeconds | default 5 }}
            periodSeconds: {{ (.Values.cornflow.probes.readiness).periodSeconds | default 5 }}
            timeoutSeconds: {{ (.Values.cornflow.probes.readiness).timeoutSeconds | default 3 }}
            failureThreshold: {{ (.Values.cornflow.probes.readiness).failureThreshold | default 3 }}
          resources:
            {{- toYaml .Values.cornflow.resources | nindent 12 }}
          env:
            {{- range $key, $value := .Values.cornflow.env }}
            - name: {{ $key }}
              value: {{ $value | replace "RELEASE_NAME" $.Release.Name | quote }}
            {{- end }}

          volumeMounts:
            - name: logs
              mountPath: /usr/src/app/log
            - name: ssh-keys
              mountPath: /usr/src/app/.ssh
              readOnly: true
            {{- if or (not (hasKey .Values.cornflow "mountAppEmptyDir")) .Values.cornflow.mountAppEmptyDir }}
            - name: app-dir
              mountPath: /usr/src/app
            {{- end }}
            {{- if .Values.cornflow.extraVolumeMounts }}
            {{- tpl (toYaml .Values.cornflow.extraVolumeMounts) . | nindent 12 }}
            {{- end }}
      {{- with .Values.cornflow.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.cornflow.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.cornflow.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: logs
          emptyDir: {}
        - name: ssh-keys
          secret:
            secretName: {{ .Release.Name }}-ssh-keys
            optional: true
        {{- if or (not (hasKey .Values.cornflow "mountAppEmptyDir")) .Values.cornflow.mountAppEmptyDir }}
        - name: app-dir
          emptyDir: {}
        {{- end }}
        {{- if .Values.cornflow.extraVolumes }}
        {{- tpl (toYaml .Values.cornflow.extraVolumes) . | nindent 8 }}
        {{- end }}